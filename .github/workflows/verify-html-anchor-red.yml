name: verify-html-anchor-red
on:
  push:
    branches: [review/html-anchor-recovery-red-147]

jobs:
  red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo test --locked --test html anchor
