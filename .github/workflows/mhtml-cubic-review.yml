name: MHTML review regressions

on:
  push:
    branches: [verify/mhtml-cubic-review]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.88.0
      - run: cargo test --locked --test mhtml
