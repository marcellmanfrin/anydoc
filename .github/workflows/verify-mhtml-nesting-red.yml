name: verify-mhtml-nesting-red
on:
  push:
    branches: [review/mhtml-cubic-nesting-red]

jobs:
  red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo test --locked --test mhtml_nesting
