name: verify-mhtml-followup-p3-red2
on:
  push:
    branches: [review/mhtml-followup-p3-red2]
permissions:
  contents: read
jobs:
  red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: 8497a052133b659c047d083d5ed016af3cd117b6
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo test nested_anchor_repair_preserves_structural_depth_in_preflight --lib -- --exact --nocapture
